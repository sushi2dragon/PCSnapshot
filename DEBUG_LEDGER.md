# PC Snapshot — Debug Ledger

Companion to `DEBUG_PLAN.md`. One row per issue. Nothing is "done" until its row
has a verification note (behavior re-checked, not just a green build).

Severity: **S1** crash/data-loss/kills-protected-process · **S2** wrong result /
dropped event / off-screen window · **S3** cosmetic / style / micro-efficiency.

---

## Baseline health — 2026-07-12 (Day 1 sweep, executed)

| Suite | Command | Result |
|---|---|---|
| Frontend lint | `npm run lint` | ✅ clean, 0 issues |
| Frontend build | `npm run build` (`tsc -b && vite build`) | ✅ built in 9.35s (only a cosmetic Vite plugin-timings note) |
| Companion tests | `npm run test:companion` | ✅ 3/3 pass |
| Rust tests | `cargo test` (from `src-tauri/`) | ✅ 18 pass, 0 fail, 1 ignored |
| Rust lint | `cargo clippy --all-targets` (from `src-tauri/`) | ⚠️ 0 errors, 21 style warnings (all S3) |

Notes:
- `cargo` target correctly redirects to `C:/cargo-build/` (OneDrive rule active).
- The 1 ignored Rust test is `browser::tests::real_browser_session` — needs a live
  browser; belongs to the Day 4 on-device pass, correctly `#[ignore]`d.
- **The tree is buildable and test-green.** The debugging week starts from a clean
  baseline, so anything found from here is a genuine regression/gap, not noise.

---

## Open items

| id | area | sev | issue | repro / note | fix | verified |
|---|---|---|---|---|---|---|
| L-001 | rust/style | S3 | 21 clippy warnings (`map_or`, `&PathBuf`→`&Path`, manual char comparison, boolean simplification, doc-list indentation) | `cargo clippy --all-targets` from `src-tauri/`; 13 auto-fixable via `cargo clippy --fix --lib -p pc-snapshot` | — | — |
| L-002 | tests/rust | S2-risk | `capture.rs` has **no unit tests** — untested core of capture pipeline | Day 2 | — | — |
| L-003 | tests/rust | S2-risk | `context.rs` has **no unit tests** — untested clue extraction | Day 2 | — | — |
| L-004 | tests/rust | S2-risk | `config.rs` / `activity.rs` have no unit tests | Day 5 / Day 6 | — | — |
| L-005 | tests/frontend | S2-risk | **no frontend test runner exists** (no `*.test.tsx`, no test script) | Day 6 — stand up Vitest | — | — |
| L-006 | rust/crash | S1-risk | densest `unwrap/expect/panic` cluster is in `browser_bridge.rs`, on the untrusted named-pipe input path | Day 4/7 audit | — | — |
| L-007 | process/git | — | large UI/activity/vscode workstream uncommitted — no tracking baseline for regressions | **needs user go-ahead to commit** | — | — |
| L-008 | frontend/boot | S2 | app stuck on "Loading PC Snapshot…" splash after system sleep. On resume, WebView2 trims/terminates the renderer and re-navigates; in dev the Vite module fetch can fail with no retry, so `main.tsx` never runs and the static `#boot` splash (index.html) stays up forever. Prod (local files) rarely trips it. | leave `npm run tauri dev` open, sleep the machine, resume | boot self-heal in `index.html`: capture-phase resource-error listener + 15s mount-timeout backstop → bounded (max 3, sessionStorage) `location.reload()` | ✅ happy path re-verified in browser (React mounts, splash replaced, no spurious reload); prod build preserves watchdog; guard predicate table all-correct (script/css→reload, favicon/img/runtime/mounted→no reload) |
| L-009 | browser/companion | S2 | "Browser Companion did not respond for N profile(s)" warning + 0 tabs captured even with a browser open. Native host (`pc_snapshot_native_host`) blocks on the *pipe*, not stdin, so when a browser closes it keeps running and holds its bridge pipe open. The bridge still counts it as a connected profile and sends capture requests it can never answer → 1.2s timeout → warning. Zombies accumulate across the app's uptime (explains "3 profiles" with one browser open). | open+close a couple browser windows, then capture with one browser still open | native host now `std::process::exit(0)` on stdin EOF / broken port, so the pipe drops and the bridge's existing disconnect path removes the session. **Not** added: blind prune-on-timeout — would drop a live-but-slow MV3 profile (service-worker cold-start >1.2s); #1 clears true zombies structurally so pruning is unneeded and risky. | ⏳ compiles clean; runtime proof needs rebuilt host re-registered + live browser (Day-4 on-device). Existing zombies clear on desktop-app restart (in-memory sessions reset). |
| L-010 | browser/companion | S2 | capture returns 0 tabs (same warning) even with the extension installed & active; only a manual "Reload" of the extension fixes it, temporarily. Root cause: MV3 service worker (`background.js`) is terminated after ~30s idle, dropping the native connection, and it only ever calls `connectNative()` on install/startup/SW-load — nothing reconnects. Manual reload restarts the SW, which re-connects; capture works only in that window. | install companion, leave browser idle >30–60s, take a snapshot | (a) `chrome.alarms` keepalive (`periodInMinutes: 0.4`) that wakes the SW and reconnects with no user action; (b) native host heartbeat every 20s to keep the SW warm between alarm ticks (immune to alarm-period clamping); (c) `alarms` permission added to both manifests. | ⏳ host compiles, `test:companion` 3/3, rebuilt dist carries `alarms` + keepalive wiring. Behavioral proof (SW survives >60s idle, capture succeeds without reload) needs on-device: rebuild+re-register host, load rebuilt extension, one final reload, then verify. |
| L-010b | browser/companion | S2 | L-010's keepalive was insufficient — user still had to manually reload before capture. Deeper cause: the 30s alarm is a *floor* (Chrome clamps sub-30s periods) and the host heartbeat only flows while the desktop app is running to send it. In the normal flow (browser open, desktop app closed until you want a snapshot), the SW dies every ~30s with nothing warming it; opening the app + capturing immediately finds a dead port and an empty session map, which `capture()` reported instantly with zero grace. | close desktop app, browse for a minute, open app, capture right away | (a) extension reconnects on ordinary browser activity — `tabs.onActivated` / `tabs.onUpdated` / `windows.onFocusChanged` all call the guarded `connectNative()`, so any interaction re-establishes the port within a keystroke instead of ≤30s; (b) desktop `capture()` now polls up to 1.5s for a profile to register before declaring "not connected", covering the ~1s host reattach after the app opens. | ⏳ lib compiles clean, bridge unit test green, `test:companion` 3/3, dist rebuilt. Behavioral proof (capture succeeds without reload after the app was closed) needs on-device. |
| L-011 | browser/companion | S2 | cold-browser restore doesn't reconcile tabs: restoring a browser-containing session when the browser was **not** running launches the browser but the companion isn't connected yet, so tabs aren't restored. Root cause: `restore_one`'s connect wait was 5s, far shorter than a real Chromium cold start + MV3 SW init + native handshake (routinely >5s), so it timed out and reported "the companion extension is not connected for this browser profile". | close the browser entirely, restore a snapshot that has browser tabs | extended `restore_one` connect deadline 5s → 15s; the full wait only elapses when the companion is genuinely absent, otherwise it proceeds as soon as the freshly-launched browser's companion attaches. | ⏳ lib compiles clean, bridge unit test green. Behavioral proof (cold restore reconciles tabs) needs on-device. |

| L-012 | restore/terminal | S2 | Restoring Windows Terminal fails: "WindowsTerminal.exe … failed to launch (Access is denied. (os error 5))" for each window that needs launching. Root cause: `launch()` does `Command::new(exe_path)` with the captured path, which for WT is under `C:\Program Files\WindowsApps\…` — an ACL-locked directory CreateProcess can't execute directly. Hits every WT launch path (main loop, terminal reconciliation, no-hook fallback). | capture ≥2 WT windows, close some, restore | central remap in `launch()`: `store_app_launch_alias()` maps the `windowsterminal` stem to the `wt.exe` App Execution Alias (on PATH), leaving the `-d <cwd>` args intact. One choke point covers all launch paths. | ✅ `wt.exe` confirmed on PATH (`…\WindowsApps\wt.exe`); unit test seals the remap; all 7 restore tests green. End-to-end (restore actually opens the WT windows) needs the running app. |

| L-013 | restore/terminal | S2 | Restored Windows Terminal windows all open in the app's own directory (PC Snapshot) instead of their captured CWDs (e.g. one was in `…\ozonetel work`). Two root causes: (1) the launch used a bare `wt -d <cwd>`, which — when a WT instance is already running — is forwarded to it and the `-d` is dropped, so the new window inherits the launcher's (app's) CWD; (2) all WT windows share the generic title "Windows PowerShell", and restore matched sessions→windows *by title*, collapsing every window onto the first session's CWD. Capture itself is correct (distinct CWDs, incl. ozonetel, verified in the snapshot JSON). | capture ≥2 WT windows in different dirs, close some, restore | (1) `terminal_launch_cmd` now emits `wt -w new -d "<cwd>"` — `-w new` forces a fresh window that honors `-d`; (2) `terminal_restore_cmd` consumes a *distinct* unconsumed session per window (title match among unconsumed, else next in order), so same-titled windows keep their own dirs. | ✅ `wt -w new -d "<path with spaces>"` confirmed to land in the target dir on-device; unit test proves 3 same-titled windows map to 3 distinct CWDs; all 8 restore tests green. End-to-end (actual multi-window restore) needs the running app. |

| L-014 | capture/terminal | S2 | Terminals opened via Win+R "cmd" (or Start/double-click) and adopted into Windows Terminal by Win11's default-terminal handoff capture the WRONG CWD — all fall back to WT's launch dir. Root cause: the shell (cmd.exe) stays a child of explorer.exe, not a descendant of WindowsTerminal.exe, so `resolve_shell_cwd`'s descendant walk misses it and falls back to `cwd_from_cmdline` of the shared WT process. | Win+R "cmd", cd somewhere, capture; cwd records as the WT launch dir | added `first_adopted_console_shell()`: when the descendant walk fails for a WT window, claim the lowest-PID unclaimed shell whose parent is explorer.exe (the DefTerm-adopted console). Guarded to WT windows; standalone consoles are already claimed via self/parent so they aren't grabbed. | ⏳ compiles, 40 lib tests green. Heuristic (WT hosts all windows under one PID, so window↔shell pairing is by PID order) — captures the right *set* of dirs. Needs on-device: **re-capture** a Win+R cmd terminal and confirm its cwd, then restore. |
| L-015 | restore/terminal | S2 | Restore over-launches terminals: with 1 of 3 WT windows still running, restore opened 3 new (total 4) instead of 2. Root cause: it decides which snapshot windows are already-covered by matching the live window to a snapshot window *by title*, but a terminal's title tracks its running command, so a running window whose title drifted since capture matches nothing and gets a duplicate launched. | capture N terminals, close some, restore; the still-running one gets duplicated | cap launches per exe at `max(0, snap_count − live_count)` in the terminal reconciliation loop, independent of title matching. | ⏳ compiles, 40 lib tests green. Needs on-device confirm (restore reuses the running terminal, launches only the missing count). |

| L-016 | capture+restore/terminal | S2 | Windows Terminal sessions don't restore working dir or history — the WT restore path only did `wt -d <cwd>` (no history) and opened WT's default shell, unlike the bash/PowerShell paths which relaunch the real shell with a cwd+history script. WT can't be reliably handed a shell+script on its cmdline (verified). | restore a snapshot with WT terminals; they open blank in the wrong dir | model it on bash: capture now records the *inner* shell (`inner_shell` on `TerminalSession`, resolved via the process walk); restore relaunches that shell directly (`powershell.exe -File <script>` / `cmd.exe /K cd`), and Win11's default-terminal handoff re-hosts it in a WT window — so the shell's own startup restores cwd + history. Bare `wt -w new -d` remains the fallback when the inner shell wasn't resolved. | ⏳ compiles, 40 lib tests green. **Needs rebuild + FRESH capture** (old snapshots lack `inner_shell` and have the wrong cwds). On-device: capture WT terminals, restore, confirm cwd+history and correct shell. cmd has no persistent history (cwd only). |

| L-017 | restore/terminal | S2 | **Terminal restore did nothing** (cmd, powershell, etc). Root cause: `restore_terminal_sessions` launched only `captured − currently-running` shells *per shell type* (count-based dedup). At restore time you almost always have shells open — in dev the terminal running `npm run tauri dev` is itself counted (measured `powershell=1` live) — so the deficit collapsed to 0 and nothing launched. It also dedup'd by shell *type* only, so it couldn't tell a running powershell in dir X from a captured one in dir Y and silently suppressed the restore. (Same mechanism that overcorrected after L-015.) Capture, `tokenize`, and the launch path were all verified sound: a real schema-4 snapshot has correct shell/exe/cwd/history, and spawning cmd+powershell exactly as restore does opens visible console windows. The "lot of errors" in the terminal were the 7 benign dead-code `warning:`s from the L-016 rework, not runtime errors. | capture terminals, restore without closing them → nothing opens | replaced the count-dedup with a **reconcile-to-captured-set** pass: `running_interactive_shells()` lists live shells with pid+cwd (same discovery/CWD source as capture); `plan_terminal_reconcile()` matches captured↔live by shell+cwd (folder case-insensitive, trailing-sep-normalized) — unchanged terminals are kept (not relaunched), missing ones relaunch fresh at saved cwd+history, and on a **clean restore** (`close_others`) live terminals the snapshot doesn't contain are terminated (except this app's own tree). Also excluded terminal-classified windows from `close_windows_not_in_snapshot` Set A so the window-level pass can't close a terminal the process pass just relaunched. PowerShell's process CWD reads as its launch dir, so a PS session with a real cwd relaunches rather than matches — intended. | ⏳ compiles clean; 44 lib tests green incl. 4 new `terminal_reconcile_*` (keep-unchanged / relaunch-missing / close-extras / plain-restore-never-closes+protect-own-tree / one-live-satisfies-one). Needs on-device: restore a schema-4 snapshot and confirm terminals reopen at their dirs, an unchanged one is kept, and (clean restore) extras close. |

| L-018 | restore/terminal | S2 | **Restored terminals flash open then close.** Once L-017 launched them, cmd/powershell windows appeared then vanished ~1-2s later. `[TERMDBG]` proved the shells spawned `alive` (not killed by the close pass) but exited on their own — impossible for `cmd /K` / `powershell -NoExit` unless the console dies. Root cause: `launch_terminal` hand-rolled `CreateProcess` with `CREATE_NEW_CONSOLE` from this **GUI (console-less) process**; Win11's default-terminal handoff to Windows Terminal drops that freshly-allocated console mid-handoff and tears the shell down. (The L-016 rework's launch path was never confirmed on-device — this is why.) | restore any snapshot with terminals; they flash and close | rewrote `launch_terminal` to launch via `ShellExecuteW("open", exe, params)` — the same path the OS uses from Win+R, which allocates a real interactive console and drives the DefTerm handoff correctly. | ✅ verified: standalone `ShellExecuteW` probe keeps cmd+powershell alive >3s (vs the old spawn dying); **and end-to-end in the app** — user restored, both windows opened and stayed, `[TERMDBG] ShellExecuteW -> 42` for both. |
| L-019 | capture/terminal | S2 | **Git Bash never captured.** A mintty/git-bash terminal produced no `terminal_session`, so restore couldn't reopen it. Root cause: the process-based capture (L-016) finds shells whose parent is a launcher, but mintty's `bash` child routinely has a **severed parent link** (recorded parent PID points at an exited process), so `interactive_shells()` never sees it. Confirmed on-device: `bash.exe` `parent=GONE`; the two `OpenConsole` helpers have no child shells. | open git-bash, capture; no bash session in the JSON | added a mintty-**window** pass to `capture_terminal_sessions`: parse the cwd from the mintty title (`MINGW64:/c/…` → Windows path, existing `git_bash_cwd_from_title`), resolve the launcher from the owning mintty exe, dedup against any bash the process walk found. | ⏳ compiles; 47 lib tests green; verified the live mintty title is in the expected format and its pid maps to mintty.exe (so a fresh capture will record it). Needs on-device: **fresh capture** with git-bash open → confirm the session appears → restore reopens it. |
| L-020 | restore/terminal | S2 | **Per-app "restore Windows Terminal" did nothing** but reported success. `snapshot_for_app` keeps sessions where `session_matches_executable(session, WindowsTerminal.exe)` is true, but the new capture tags sessions by inner shell (`powershell`/`cmd`), which never matched the `windowsterminal` stem → empty session list. | select Windows Terminal in the per-app list, restore | rewrote `session_matches_executable`: a console host (`windowsterminal`/`wt`/`conhost`/`openconsole`) matches all its console shells (`powershell`/`pwsh`/`cmd`, plus legacy `windows_terminal` for schema-3 snapshots); `mintty` matches `git_bash`; a directly-selected shell exe matches its own shell. | ⏳ compiles; new unit test `per_app_restore_maps_host_to_its_shells` green; 47 total. Needs on-device confirm. |
| L-021 | restore/terminal | S3 | **"Restored session history" is misleading (PS/bash).** PowerShell/Git Bash write one global history file shared across all sessions, so the restored block shows mostly-unrelated old commands (only the last few are this session's). Inherent — no per-window history exists for those shells. | restore a powershell terminal; history block shows old commands | relabeled the block "Recent history (shared across … sessions)" in both the PS and bash restore scripts so it stops claiming to be this session's history. | ✅ label-only; test updated. |
| L-022 | capture+restore/terminal | S2 | **cmd history now captured (was: "cmd has no history").** Earlier assumption that cmd history is unrecoverable was wrong: cmd keeps no *file*, but the console server holds the per-session buffer (what `doskey /history` shows), readable via `AttachConsole(pid)` + the name-exported-but-undocumented `GetConsoleCommandHistoryW`. Verified on-device it reads a live **WT-hosted** cmd's history correctly. Unlike PS/bash, this is genuinely *per-session* (accurate, not global). | (feature) capture a cmd, restore it, want to see its history | capture: `read_cmd_console_history(pid)` briefly attaches to the target's console and reads the buffer (FreeConsole before/after — process-global). restore: a `.cmd` script (run via `/K`) prints the history from a sidecar file with `type` (so command text with `& % |` isn't reparsed) then cd's. Up-arrow replay is *not* attempted (no clean API to seed a new console's history; would need a clink-style hook). | ⏳ compiles; 47 lib tests green. Read API verified on live WT cmd; restore-script rendering verified (special chars safe, cd correct). Needs on-device: fresh capture of a cmd → restore → confirm its real commands display. |

Add rows as issues are found. Move nothing to "verified" without a behavior re-check.
